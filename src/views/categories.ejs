<%- include('partials/header') %>

    <main>
        <h1>
            <%= title %>
        </h1>

        <ul>
            <% categories.forEach(function(category) { %>
                <li>
                    <%= category.name %>
                </li>
                <% }); %>
        </ul>
    </main>

    <%- include('partials/footer') %>